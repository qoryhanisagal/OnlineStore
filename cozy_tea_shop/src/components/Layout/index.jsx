// Re-exporting the Layout component to simplify imports elsewhere in the app.
// Allows us to import from 'components/Layout' instead of 'components/Layout/Layout'
export { default } from './Layout';