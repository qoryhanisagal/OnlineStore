// This index.jsx file is used to re-export the Footer component.
// I’m using this to simplify imports across the app so I can just import from the folder
// instead of specifying the full file path (e.g., 'Footer' instead of 'Footer/Footer').

export { default } from './Footer';